<Window x:Class="PopupGuard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PopupGuard - 弹窗监控工具" Height="600" Width="1200" 
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- 现代按钮样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 次要按钮样式 -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 危险按钮样式 -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#F44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 操作按钮样式 -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代复选框样式 -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <!-- 现代组合框样式 -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="4,0"/>
        </Style>
        
        <!-- 列表视图样式 -->
        <Style x:Key="ModernListView" TargetType="ListView">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- 列表项样式 -->
        <Style x:Key="ModernListViewItem" TargetType="ListViewItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F0F0F0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8F9FA"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#1976D2"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsTrusted}" Value="True">
                    <Setter Property="Foreground" Value="#666"/>
                </DataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsTrusted}" Value="False"/>
                        <Condition Binding="{Binding Publisher}" Value=""/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Foreground" Value="#D32F2F"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- 状态指示器样式 -->
        <Style x:Key="StatusIndicator" TargetType="Border">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="4,0"/>
        </Style>
    </Window.Resources>
    
    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="16,12">
    <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 左侧状态信息 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Style="{StaticResource StatusIndicator}" Background="#4CAF50"/>
                    <TextBlock Text="正在监听窗口事件..." FontWeight="SemiBold" Foreground="#333" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
                
                <!-- 右侧操作按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🎯 取窗" Style="{StaticResource ModernButton}" Click="OnPickClick"/>
                    <CheckBox Content="只看取窗" Style="{StaticResource ModernCheckBox}" IsChecked="{Binding OnlyPicked, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="8,0">
                        <TextBlock Text="批次:" VerticalAlignment="Center" Foreground="#666" FontSize="12"/>
                        <ComboBox Width="80" Style="{StaticResource ModernComboBox}" 
                                  ItemsSource="{Binding BatchIds, RelativeSource={RelativeSource AncestorType=Window}}" 
                                  SelectedItem="{Binding BatchFilter, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay}" 
                                  IsEditable="False"/>
                        <Button Content="清空" Style="{StaticResource SecondaryButton}" Click="OnClearBatchFilterClick"/>
                    </StackPanel>
                    
                    <Button x:Name="ExportCsvButton" Content="导出CSV" Click="OnExportCsvClick" 
                            Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                    <Button x:Name="TestButton" Content="测试弹窗" Click="OnTestClick" 
                            Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                    <Button x:Name="SettingsButton" Content="设置" Click="OnSettingsClick" 
                            Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                    <Button Content="📋 规则" Style="{StaticResource SecondaryButton}" Click="OnRulesClick"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 选中项信息栏 -->
        <Border Grid.Row="1" Background="#E8F5E8" BorderBrush="#C8E6C9" BorderThickness="0,0,0,1" 
                Padding="16,8" Margin="0" 
                Visibility="{Binding LastPickedSummaryVisible, RelativeSource={RelativeSource AncestorType=Window}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📍" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding LastPickedSummary, RelativeSource={RelativeSource AncestorType=Window}}" 
                           Foreground="#2E7D32" FontWeight="Medium"/>
            </StackPanel>
        </Border>
        
        <!-- 主数据表格 -->
        <ListView Grid.Row="2" x:Name="EventsList" Style="{StaticResource ModernListView}" 
                  Margin="16" ItemContainerStyle="{StaticResource ModernListViewItem}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="标记" Width="40">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="12" Height="12" Background="{Binding BatchColor}" 
                                        CornerRadius="2" Margin="4,0"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="来源" Width="80" DisplayMemberBinding="{Binding Source}"/>
                    <GridViewColumn Header="批次" Width="60" DisplayMemberBinding="{Binding BatchId}"/>
                    <GridViewColumn Header="时间" Width="100" DisplayMemberBinding="{Binding Time}"/>
                    <GridViewColumn Header="进程ID" Width="80" DisplayMemberBinding="{Binding ProcessId}"/>
                    <GridViewColumn Header="软件名称" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,6,0" 
                                           Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" 
                                               ToolTip="{Binding ProcessName}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="公司" Width="160" DisplayMemberBinding="{Binding CompanyName}"/>
                    <GridViewColumn Header="发布者" Width="160" DisplayMemberBinding="{Binding Publisher}"/>
                    <GridViewColumn Header="路径" Width="250" DisplayMemberBinding="{Binding ExecutablePath}"/>
                    <GridViewColumn Header="标题" Width="200" DisplayMemberBinding="{Binding WindowTitle}"/>
                    <GridViewColumn Header="操作" Width="280">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="关闭" Style="{StaticResource ActionButton}" Click="OnCloseClick"/>
                                    <Button Content="强制结束" Style="{StaticResource DangerButton}" Click="OnKillClick"/>
                                    <Button Content="打开位置" Style="{StaticResource ActionButton}" Click="OnOpenClick"/>
                                    <Button Content="屏蔽" Style="{StaticResource ActionButton}" Click="OnBlockClick"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
